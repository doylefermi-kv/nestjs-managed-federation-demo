type Post {
  id: ID!
  title: String!
  body: String!
  author: User
}

extend type User @key(fields: "id") {
  id: ID! @external
  posts: [Post]
}

extend type Query {
  getPost(id: ID!): Post!
  getPosts: [Post]
}
